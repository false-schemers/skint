(define-library (srfi 48)
  (export format)
  (import (scheme base) (scheme write)
          (only (skint) 
            format format-pretty-print
            format-fresh-line format-help-string))

(begin

;; TODO: install simplistic pretty printer

))
